<UserControl x:Class="Iz.SendmailTool.AppBase.Controls.InputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Iz.SendmailTool.AppBase.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="500">
    <Grid Background="#FF448884" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0"    FontWeight="Bold" Foreground="Black"
                 Content="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}">
        </Label>

        <TextBox Grid.Column="1"  Margin="5" TextWrapping="Wrap"  
                 Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, 
                        Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
        VerticalContentAlignment="Top" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
    </Grid>
</UserControl>
